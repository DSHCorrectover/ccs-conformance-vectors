# Case 10 — Chain Negatives

Tests chain integrity: sequence contiguity, prev-hash linkage,
trace-id consistency, empty-chain detection, and prev-hash declaration
anchoring (declarations must match the computed predecessor digest, not each
other).
