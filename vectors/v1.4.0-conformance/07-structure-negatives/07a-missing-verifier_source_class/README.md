# Case 07a — Missing field: `verifier_source_class`

The field `verifier_source_class` is removed from an otherwise-valid L1 receipt.
Strict mode requires exactly the 30 defined fields; the checker must reject.
